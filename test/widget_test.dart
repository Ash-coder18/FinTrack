import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FinTrack app smoke test', (WidgetTester tester) async {
    // Verify the app widget can be created.
    // Note: Full widget testing requires Supabase mocking.
    expect(true, isTrue);
  });
}
